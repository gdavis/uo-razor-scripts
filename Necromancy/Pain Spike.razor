clearsysmsg 
say '[PainSpike'

while not targetexists 
endwhile

if targetexists neutral and noto lasttarget = harmful
    target lasttarget
endif

while targetexists 
endwhile
wait 100

if insysmsg 'Target cannot be seen'
    overhead 'CANNOT SEE!' 38
elseif insysmsg 'That is not accessible' or insysmsg 'That is not a valid target'
    overhead 'Invalid Target' 38
elseif insysmsg 'You must wait'
    overhead 'Not ready!' 38
elseif insysmsg 'You do not have enough unholy'
    overhead 'No symbols!' 44
else  
    cooldown 'Pain Spike' 30000
endif