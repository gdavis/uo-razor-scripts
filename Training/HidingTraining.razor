if insysmsg 'The world will save'
   overhead 'Waiting for world save...' 

    if insysmsg 'World save complete'
        overhead 'Save complete, continuing...'
        clearsysmsg 
        loop
    endif
    
    wait 1000
    loop
endif

if not hidden
    skill 'Hiding'
    wait 500
    if insysmsg 'You have hidden'
        wait 11000
    else 
        wait 8500
    endif
endif

walk north
walk north
wait 250
walk south
walk south


if not timerexists 'guards'
    createtimer 'guards'
endif

if timer 'guards' > 20000
    say 'guards'
    settimer 'guards' 0
endif

loop