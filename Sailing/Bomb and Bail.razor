if findtype 'barrel' self 2825 as bomb
    dclick bomb
    wait 200
    
    if insysmsg 'You cannot use ship bombs on board friendly ships'
        overhead 'Friendly ship!'
        stop
    endif
    
    overhead 'BOMB PLACED!' 54
    cooldown 'Bomb'
else
    overhead 'No large bombs!' 38
endif

if varexist shipTillerman
    script 'Sailing\Embark All'
else
    say '[Embark'
    wait 150
    say '[EmbarkFollowers'
    wait 150
    say '[RecallBoardingParty'
endif