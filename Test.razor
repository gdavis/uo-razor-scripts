#etvar! 'success' 1
#
#if 'chopped' == 2
#    overhead '%s' 'chopped'
#else 
#    overhead 'nothing'
#endif

if not findbuff 'campfire visit'
    overhead 'Need to camp'
else 
    overhead 'Have camp buff'
endif